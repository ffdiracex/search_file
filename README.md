# search_file


Compile: 

gcc walk.c -o walk -std=gnu18

NOTE:
since we have defined _POSIX_C_SOURCE_200809L we have made the code POSIX compatible, thus we don't need to specifiy this with a compiler flag
